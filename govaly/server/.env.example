# Copy to .env and adjust as needed.
# Leave MONGO_URI empty to auto-start an in-memory MongoDB (dev/demo mode).
PORT=5000
MONGO_URI=
JWT_SECRET=change-me-to-a-long-random-string
NODE_ENV=development
# Refresh tokens are signed with JWT_SECRET + '-refresh'; both secrets should be long random strings.
